See your entries in a new light.

• New: Brain view — an interactive map of the people and themes
  running through your entries, built entirely on-device. Unlocks
  after 10 entries.

• Fixed: the subscription screen showed a fake "Deep" purchase badge
  — now shows an honest "Early Access" status instead.

• Fixed: the word-goal progress bar tracked all words written that
  day instead of the entry you're on — now tracks per entry.

• Fixed: entries missing a mood tag now get backfilled automatically.

• Performance and stability improvements.
