# mirror-loop state

The agent forgets each run. This file does not — read it before starting.

## Backlog

Seed source: SwiftUI `var body`/computed properties doing per-frame `.sorted`/`.filter`/`Dictionary(grouping:)`
work with no `.task`-cached equivalent (same bug shape as the fixes already benchmarked in
`mirrorTests/PerformanceXCTests.swift`). Re-scan `Features/**/*.swift` for this pattern before assuming the list below is complete.
Lesson from Log [10]: the grep for `.sorted(`/`.filter { }`/`Dictionary(grouping:`/`decrypt` is not
sufficient on its own — a hand-written `for` loop doing the equivalent work (dedup, streak-walk,
front-scan) won't match those keywords. Files that already have ONE `.task(id:)` cache are not safe
to skip: check every computed property in the file, not just the ones the grep flagged, especially
if a sibling property reads the same full-history `@Query` uncached (see `MoodTimelineView.swift`,
which cached its heatmap chain in Log [1] but missed `currentStreak`/`consecutiveNegativeCount`
right below it).

Candidates found 2026-07-06, not yet done:
- `Features/Insights/InsightView.swift:401` — `dominantMoodThisWeek` (`Dictionary(grouping: moodEntries...)`), no cache
- `Features/Insights/InsightView.swift:1029` — `dominantMood` (`Dictionary(grouping: points...)`), no cache
- `Features/Insights/MonthlyReportView.swift:348` — `topMood` in `MonthlyStatsStrip`, no cache
- `Features/Entries/EntryDetailView.swift:25` — `.filter` over `allEntries` on every eval, check cost at scale

Already fixed / not backlog items (verified during discovery, do not re-add without new evidence):
- `Features/Entries/EntryListView.swift` — `listSnapshot` already `.task(id: snapshotDeps)`-cached
- `Features/Entries/CalendarHeatmap.swift` — `dayCache` already `.task(id:)`-cached
- `Features/Settings/SettingsView.swift` — already `.task(id: entries.count)`-cached
- `Features/Write/WriteView+Subviews.swift` — `dailyWordCount` removed entirely (bar is per-entry now), see Product decision below; nothing left to cache
- `Features/Insights/AskView.swift` — `askHistory`/`chatHistory`/`thisMonthCount` now cached, see Log [5]
- `Features/Insights/InsightView.swift` — `moodEntries`/`thisMonthEntries`/`currentStreak`/`pastNudges` now cached, see Log [6]
- `Features/Write/WriteView+Tags.swift` `filteredTagSuggestions` — DISCARD (2026-07-07, no code touched) — operates on `existingTagSuggestions`, a small @State array populated once via `computeTagSuggestions()` when the tag input opens (`WriteView.swift:360`), not per-keystroke; already-filtered-subset false positive
- `Features/Insights/InsightViewModel.swift` `thisMonthEntries`/etc. — DISCARD (2026-07-07, no code touched) — these run inside `async func load*` methods called from `.task`/`.onChange`, not from a view `body`; not per-frame

## Log

- [1] MoodTimelineView.swift heatmap chain (`allMoodPoints`/`dayMoodMap`/`heatmapWeeks`) recomputed on every body re-eval (range taps, paywall sheet) despite depending only on `entries`, not `selectedRange` — cached via `.task(id: entries.map(\.encryptedMood).hashValue)` into `@State` vars, matching `CalendarHeatmap.swift` precedent. Added `test_moodTimelineHeatmap_perFrameVsCached` to `PerformanceXCTests.swift`. Build gate + full mirrorTests target green. — **KEEP** (2.0.3, PR #17)
- [2] InsightView.swift:401 `dominantMoodThisWeek` — DISCARD (no code touched) — groups only current-week entries (few items), not a hot path
- [2] InsightView.swift:1029 `dominantMood` (MoodWeekChartView) — DISCARD (no code touched) — same, week-scoped subset
- [2] MonthlyReportView.swift:348 `topMood` (MonthlyStatsStrip) — DISCARD (no code touched) — one-month-scoped subset, trivial
- [2] EntryDetailView.swift:25 `onThisDayEntries` — DISCARD (no code touched) — full-history scan but runs once per detail-view open, not per-frame/animated; one-time cost is acceptable
- [3] WriteView+Subviews.swift `dailyWordCount` filtered+reduced the full-history `allEntries` @Query on every read; `toolRow` (part of `body`) reads `viewModel.wordCount` directly and that `@Observable` property updates on every keystroke while typing, so this full-history scan ran on every character typed in the editor — likely the highest-frequency instance of this bug shape found so far. Cached the saved-history portion into `@State var cachedSavedWordCountToday` via `.task(id: allEntries.count)` on `WriteView.swift`, matching `CalendarHeatmap`/`MoodTimelineView` precedent; `dailyWordCount` now reads the cache and only adds the live `viewModel.wordCount` per keystroke. Added `test_dailyWordCount_perKeystrokeVsCached` to `PerformanceXCTests.swift`. NOT build/test-verified (no Xcode/simulator in this sandbox) — awaiting local xcodebuild + mirrorTests run. — proposed (2.0.4, PR #18, branch `loop/writeview-dailywordcount-cache`)

- [4] 2026-07-07 local verify: PR #18 (WriteView dailyWordCount cache, cloud-proposed) was merged by user before local verification — ran gates retroactively: build green, mirrorTests 91/91 pass incl. new test_dailyWordCount_perKeystrokeVsCached. — KEEP (verified post-merge)
- [5] AskView.swift `chatHistory` filtered+sorted the full-history `allInsights` @Query (all `InsightType` cases, no date/range predicate) on every read; `content` (part of `body`) reads it via `ForEach(chatHistory)`, and the view holds `@State private var question`/`keyboardHeight`/`isInputFocused` that churn on every keystroke/keyboard event while the Ask chat is open, so this ran on every character typed. Cached `askHistory`/`chatHistory` into `@State` (`cachedAskHistory`/`cachedChatHistory`) via `.task(id: allInsights.count)` on `AskView.swift`, matching `CalendarHeatmap`/`MoodTimelineView`/`WriteView` precedent; `thisMonthCount` now filters the cache too. Added `test_askViewChatHistory_perKeystrokeVsCached` to `PerformanceXCTests.swift`. NOT build/test-verified (no Xcode/simulator in this sandbox) — awaiting local xcodebuild + mirrorTests run. — proposed (2.0.4, PR #19, branch `loop/askview-chathistory-cache`)

- [6] 2026-07-07 InsightView.swift: `moodEntries`, `thisMonthEntries`, and `currentStreak` each scan the full-history `entries` @Query with no date/range predicate already applied, and `pastNudges` filters+sorts the full `insights` @Query — all four are read directly from `body`/its always-evaluated section subviews (`nudgeSection`, `explorationSection`, `pastNudgesSection` guard, `MoodWeekChartView`), so every unrelated `@State` toggle in this view (`nudgeExpanded`, `digestExpanded`, `pastNudgesExpanded`, any sheet) re-ran all four from scratch, some multiple times per single body eval. This refines the earlier (2026-07-06, Log [2]) `dominantMoodThisWeek` DISCARD: that assessment only looked at the `Dictionary(grouping:)` step in isolation (correctly cheap, small week-scoped input) and didn't examine that its input (`moodEntries`) is itself an uncached full-history scan read directly from `body`. Cached all four into `@State` (`cachedMoodEntries`/`cachedThisMonthEntries`/`cachedCurrentStreak`/`cachedPastNudges`) via `.task(id: entries.count)` / `.task(id: insights.count)`, matching `CalendarHeatmap`/`MoodTimelineView`/`WriteView`/`AskView` precedent. Added `test_insightViewBodyRecompute_perToggleVsCached` to `PerformanceXCTests.swift`. NOT build/test-verified (no Xcode/simulator in this sandbox) — awaiting local xcodebuild + mirrorTests run. — proposed (2.0.4, PR #20, branch `loop/insightview-fullhistory-cache`)

- [7] 2026-07-07 local verify: PR #19 (AskView chatHistory cache) — build + full mirrorTests green on the branch, incl. new `test_askViewChatHistory_perKeystrokeVsCached`. Merged into 2.0.4 (`6b7fa39`). — **KEEP** (verified pre-merge)
- [8] 2026-07-07 local verify + fix: PR #20 (InsightView full-history caches) — review found `.task(id: entries.count)` misses in-place edits (mood set on an existing entry, or its date changed, doesn't change `entries.count`, so `cachedMoodEntries`/`cachedThisMonthEntries`/`cachedCurrentStreak` stayed stale — a regression vs the old uncached recompute). Fixed on the branch before merge: keyed on `entryCacheKey` (Hasher over `entries.count` + each entry's `encryptedMood` + `createdAt`), matching the PR #17 MoodTimelineView precedent; `insights.count` key kept for `pastNudges` (nudges append-only). Rebased onto post-#19 2.0.4, resolved `PerformanceXCTests.swift` append-conflict (both tests kept, second renumbered Test 8). Build + full mirrorTests green incl. both new benchmarks. Merged into 2.0.4 (`0302b61`). — **KEEP** (verified pre-merge, with fix). Outer-loop lesson: for `.task(id:)` caches over @Query data, `count` is only a valid key when the underlying rows are append-only AND the cached computation reads no mutable fields; if it reads editable fields (mood, createdAt), hash those fields into the key.

- [9] 2026-07-07 fresh re-scan of `Features/**/*.swift` (per Status note above) — grepped all files for `.sorted(`/`.filter { }`/`Dictionary(grouping:`/`decrypt`. All 14 hits traced: `InsightView.swift`/`AskView.swift`/`MoodTimelineView.swift`/`EntryListView.swift`/`CalendarHeatmap.swift` are the already-cached files from Log [1]/[5]/[6] (confirmed unmodified since those fixes via `git log`); `InsightViewModel.swift`/`MonthlyReportView.swift`/`EntryDetailView.swift` are prior DISCARDs, unchanged. Three files not previously logged were checked and are non-candidates: `SettingsView+Sections.swift:912` (`.filter` on an import-parsed text blob, not a `body`-read `@Query`), `NoteEditorTextView.swift:1923` (UITextView delegate logic over the currently-open document, not full-history), `NoteEditorTypes.swift:133` (`.filter` over a single `String`), `WriteView+Actions.swift:264` (one-time draft-restore decrypt, not per-frame), `WhatsNewSheet.swift:135,151` (`.filter` over `service.allCards`, a small static feature-card list, not an `@Query`). Also confirmed the unrelated same-day commit `fc70afb` (mood backfill on app-active/nightly passes) touches only `mirrorApp.swift`/`LocalLLMService.swift`/`WriteView+MoodDetection.swift`, none of which are view-body full-history scans. No new candidates found. No PR opened. — nothing found (2.0.4)

- [10] 2026-07-08 fresh re-scan of `Features/**/*.swift` — grep for `.sorted(`/`.filter { }`/`Dictionary(grouping:`/`decrypt` returned the same 14 files as Log [9] (nothing changed in `Features/` since `57baf07`, confirmed via `git log`). Went one level deeper this time and found a genuine miss in `MoodTimelineView.swift:125-168` that the earlier grep-only passes overlooked because these two properties don't contain the grep'd keywords themselves (manual `for` loops, not `.sorted`/`.filter`/`Dictionary(grouping:`): `currentStreak` (dedup+streak-walk) and `consecutiveNegativeCount` (front-scan) both iterate the full-history `entries` @Query (not `filteredEntries`, which is the range-scoped one this same view already uses elsewhere) and are read unconditionally from `statsRow`/`moodAlertBanner`, both always part of `mainContent`. This view's own `selectedRange` (mutated on every `rangeSelector` tap) and `showPaywall` sheet toggle re-eval `mainContent`'s body on every interaction, re-running both full-history scans each time even though neither depends on that state — same bug shape as Log [1]'s heatmap chain in this same file, and notably this file already has a `.task(id:)` cache for that heatmap chain (`cachedAllMoodPoints`/`cachedDayMoodMap`/`cachedHeatmapWeeks`, line 224) but these two properties were never folded into it. This is also literally the same streak algorithm already fixed in `InsightView.swift` (Log [6]/[8]), just never applied here. Cached both into `@State` (`cachedCurrentStreak`/`cachedConsecutiveNegativeCount`) via `.task(id: entryCacheKey)`, reusing the InsightView `entryCacheKey` pattern (hash of `entries.count` + each entry's `encryptedMood` + `createdAt`, not just count, per the Log [8] lesson — `consecutiveNegativeCount` reads `mood` and both read `createdAt`). Added `test_moodTimelineStreakAndNegativeCount_perTapVsCached` to `PerformanceXCTests.swift` (Test 9). NOT build/test-verified (no Xcode/simulator in this sandbox) — awaiting local xcodebuild + mirrorTests run. — proposed (2.0.4, PR #21, branch `loop/moodtimeline-streak-cache`)

- [11] 2026-07-08 second pass this run: PR #21 (Log [10]) confirmed still open/unmerged via GitHub (`list_pull_requests`), not yet build/test-verified locally. Checked current `2.0.4` HEAD directly — `MoodTimelineView.swift`'s `currentStreak`/`consecutiveNegativeCount` are still uncached, confirming the fix lives only on branch `loop/moodtimeline-streak-cache` and PR #21 hasn't been superseded or partially applied elsewhere. Two commits landed in `Features/` since the Log [10] scan point (`b17fe0d` gates `manageSection` on `SubscriptionService.allFeaturesFree`, `b843321` adds `earlyAccessSection`) — both confined to `Features/Settings/SubscriptionView.swift`, pure UI copy/visibility changes, no `@Query` reads or computed properties over entry/insight history, not candidates. Re-ran the grep seed as a sanity check; same 14 hits as Log [9]/[10], all previously triaged (cached or DISCARD). No new candidate found, no new PR opened; PR #21 remains the one pending item awaiting human local xcodebuild/mirrorTests verification. — nothing found (2.0.4)

- [12] 2026-07-08 third pass this run: PR #21 still open/unmerged (confirmed via `list_pull_requests`); no commits landed under `Features/` since Log [11]'s check point (`git log b00ae1e..HEAD -- Features/` empty). Went beyond the keyword grep this time and enumerated every `@Query` declaration in `Features/**/*.swift` directly (`grep -rn "@Query"`), then read the full source of every file with a full-history `@Query` that hadn't been read end-to-end in a prior pass: `MonthlyReportView.swift` (both `entries` and `insights` queries), `EntryDetailView.swift`, and `WeeklyDigestView.swift` (not previously mentioned in this log at all). Findings: `WeeklyDigestView.swift`/`DigestSectionView` only ever parses a single `Insight.content` string, no `@Query` involved — not a candidate, same shape as the existing `NoteEditorTypes.swift` DISCARD. `EntryDetailView.swift` re-checked in full (not just `onThisDayEntries`): its only other `@Query`-derived reads are `.task(id: entry.id)`-scoped (`relatedInsight`/`displayedWordCount`, computed once per detail-view open, not per-frame); confirms the existing DISCARD still holds, no sibling miss like the one found in `MoodTimelineView.swift` at Log [10]. `MonthlyReportView.swift` has two properties reading full-history `@Query`s directly from `body`/`reportContent` — `selectedMonthEntries` (`entries.filter` to the selected month) and `cachedReportForSelectedMonth` (`insights.first` linear scan) — but the view's only `@State` besides `selectedMonth` is `showPaywall`, and `showPaywall` only becomes true from `deepLockedCard`, a branch that never reads either property (Swift computed properties are lazy per `switch` branch, so toggling the sheet there never re-triggers the scan); there is no frequently-churning unrelated `@State` (no text input, no per-keystroke `@Observable`, no animation flag) in this view, so it doesn't match the bug shape's third condition — logging as reviewed-and-discarded rather than leaving it un-triaged. `MoodTimelineView.swift`'s `moodDistribution`/`averageMoodScore` (adjacent to the Log [10] fix) were also re-checked and confirmed to read the already-range-scoped `moodEntries`/`points` chain, not the raw `entries` query — correctly out of scope; PR #21 already covers the only two full-history-scanning properties in that file. No new candidate found, no new PR opened. — nothing found (2.0.4)

- [13] 2026-07-08 fourth pass this run: PR #21 (Log [10]) confirmed still open/unmerged via `list_pull_requests` (created 2026-07-08T00:08Z, no activity since). Zero commits landed under `Features/` since Log [12]'s checkpoint (`git log b00ae1e..HEAD -- Features/` empty; only new commits are two more loop-state log entries). Re-ran both scan methods as a sanity check rather than trusting the stale backlog: the keyword grep (`.sorted(`/`.filter { }`/`Dictionary(grouping:`/`decrypt`) returned the same hits across the same files as Log [9]-[12]; the `@Query` inventory (`grep -rn "@Query"`) returned the same 8 files as Log [12] plus `OnboardingFlow.swift`, which has a single `@Query private var profiles: [UserProfile]` — not entry/insight history, no `.sorted`/`.filter`/`Dictionary(grouping:)`, not a candidate. No file content changed since Log [12], so no new candidate is possible without new evidence; found nothing, no new PR opened. — nothing found (2.0.4)

- [14] 2026-07-09 fifth pass this run: PR #21 (Log [10]) confirmed still open/unmerged via `list_pull_requests` (created 2026-07-08T00:08Z, still no activity/reviews as of this run). `git log b00ae1e..origin/2.0.4 --stat -- Features/` is empty — no commits have touched `Features/**/*.swift` since Log [12]/[13]'s checkpoint; the only new commits on `2.0.4` since then are prior loop-state log entries themselves (`efcc0bd`..`eb2060f`) plus one unrelated Settings UI change (`b17fe0d`/`b843321`, `SubscriptionView.swift` visibility/copy only, no `@Query` or computed-property changes, already noted in Log [11]). Re-ran both scan methods again as a sanity check: keyword grep and `@Query` inventory (`grep -rln "@Query"`) both returned the same 9 files as Log [13] (`OnboardingFlow`/`SettingsView`/`MonthlyReportView`/`AskView`/`InsightView`/`MoodTimelineView`/`WriteView`/`EntryDetailView`/`EntryListView`), all previously triaged (cached, DISCARD, or Blocked). Since no source file has changed since the last full read-through, no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.4)

- [15] 2026-07-09 sixth pass this run (new scheduled invocation, ~6h after Log [14]): PR #21 (Log [10]) reconfirmed still open/unmerged via `list_pull_requests` (created 2026-07-08T00:08Z, `updated_at` unchanged since creation — no reviews, no comments, no pushes) and its head commit's combined status is empty (`total_count: 0`, no CI configured in this repo to report). `git log 388deb6..origin/2.0.4 --stat -- Features/` is empty — zero commits under `Features/` since the Log [14] checkpoint; the only new commit on `2.0.4` since then is Log [14]'s own log entry. Re-ran both scan methods once more: keyword grep and `@Query` inventory (`grep -rln "@Query"`) both returned the same 9 files as Log [13]/[14], all previously triaged. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.4)

- [16] 2026-07-09 seventh pass this run (new scheduled invocation): PR #21 (Log [10]) reconfirmed still open/unmerged via `list_pull_requests` (`created_at`/`updated_at` both still `2026-07-08T00:08:12Z` — zero activity, no reviews, no comments, no pushes, over 24h now). `git log 388deb6..HEAD --stat -- mirror/Features/` is empty — zero commits under `Features/` since the Log [15] checkpoint; only new commit since then is Log [15]'s own log entry (`4f3ac2b`). Re-ran both scan methods once more as a sanity check: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the same 14 files as every prior pass; `@Query` inventory (`grep -rln "@Query"`) returned the same 9 files as Log [13]-[15]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. PR #21 remains the sole pending item, still awaiting human local xcodebuild/mirrorTests verification and merge. — nothing found (2.0.4)

- [17] 2026-07-09 eighth pass this run (new scheduled invocation): PR #21 (Log [10]) reconfirmed still open/unmerged via `list_pull_requests` (`created_at`/`updated_at` both still `2026-07-08T00:08:12Z` — zero activity of any kind since creation, over 24h now with no reviews/comments/pushes). `git log 388deb6..HEAD -- mirror/Features/` is empty — zero commits under `Features/` since the Log [16] checkpoint; only new commit since then is Log [16]'s own log entry (`d90ef77`). Re-ran both scan methods once more: keyword grep and `@Query` inventory both returned the identical file sets as every prior pass (Log [13]-[16]), all previously triaged. No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. PR #21 remains the sole pending item — this is now the fourth consecutive scheduled pass to find PR #21 completely untouched since it was opened 2026-07-08. — nothing found (2.0.4)

- [18] 2026-07-10 ninth pass this run (new scheduled invocation): PR #21 (Log [10]) reconfirmed still open/unmerged via `list_pull_requests` (`created_at`/`updated_at` both still `2026-07-08T00:08:12Z` — zero activity of any kind since creation, now over 48h with no reviews/comments/pushes). `git log 388deb6..HEAD -- mirror/Features/` is empty — zero commits under `Features/` since the Log [16] checkpoint; the only commits since then are Log [16]/[17]'s own log entries (`d90ef77`, `4f3ac2b`, `a4811a9`). Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) and `@Query` inventory (`grep -rln "@Query"`) both returned the identical 9-file set as every prior pass since Log [13], all previously triaged (cached, DISCARD, or Blocked). No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. PR #21 remains the sole pending item, now completely untouched for over 48h since opening. — nothing found (2.0.4)

- [19] 2026-07-10 tenth pass this run (new scheduled invocation): PR #21 (Log [10]) reconfirmed still open/unmerged via `list_pull_requests` (`created_at`/`updated_at` both still `2026-07-08T00:08:12Z` — zero activity of any kind since creation, now well over 48h with no reviews/comments/pushes). `git log b843321..HEAD -- mirror/Features/` is empty — zero commits under `Features/` since the last real Features change (`b843321`, the SubscriptionView early-access copy change already noted in Log [11]); the only commits since then are prior loop-state log entries. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) and `@Query` inventory (`grep -rln "@Query"`) both returned the identical 9-file set as every prior pass since Log [13], all previously triaged (cached, DISCARD, or Blocked). No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. PR #21 remains the sole pending item — this is now the fifth consecutive scheduled pass to find PR #21 completely untouched since it was opened 2026-07-08 (over 2 days with zero human interaction). — nothing found (2.0.4)

- [20] 2026-07-10 local verify (human session, ends the "still pending" streak from Log [11]-[19]): PR #21 (MoodTimelineView currentStreak/consecutiveNegativeCount cache) — merged into 2.0.4 via `--no-ff` merge commit. Build green (`xcodebuild ... build`), full mirrorTests target green: 94/94 incl. new `test_moodTimelineStreakAndNegativeCount_perTapVsCached`. Also fixed a real App Store submission blocker found in the same pass: main app target `CURRENT_PROJECT_VERSION` was 1 vs widget extension's 2 (xcodebuild warns this fails validation) — bumped both to 3. Separately, committed the in-progress BrainView feature (on-device theme/people constellation graph for Insights: `BrainView`/`BrainConstellationView`/`BrainSceneView`/`BrainGraph`/`BrainViewModel` + `ThemeExtractionService`, gated behind a 10-entry minimum) into 2.0.4 — full build + mirrorTests (108/108 incl. new `BrainGraphBuilderTests`) verified green with it included. — **KEEP** (verified pre-merge, on iPhone 17 sim, Xcode 26.6)

- [21] 2026-07-10 eleventh pass this run (new scheduled invocation, after PR #21 merge in Log [20]): confirmed zero open PRs (`list_pull_requests` state=open → empty). New surface since last full scan: `Log [20]`'s BrainView feature commit (`59e6998`) added 5 files under `Features/Insights/` (`BrainView.swift`, `BrainViewModel.swift`, `BrainConstellationView.swift`, `BrainSceneView.swift`, `BrainGraph.swift`) plus `ThemeExtractionService.swift` (not in `Features/`) — none previously triaged in this log. Read all five Feature files end-to-end. `BrainView.swift` holds a full-history `@Query private var entries: [Entry]` and several churning `@State` (`is3D`, `selectedNode`, `askPrefill`, `showPaywall`); its `taskKey(size)` helper calls `BrainViewModel.fingerprint(of: entries)` (an O(n) hash over every entry's encrypted fields) on every body re-eval, but this is used solely as the `.task(id:)` identity argument — structurally identical to the already-accepted `entryCacheKey` pattern in `MoodTimelineView.swift:28-36` (also an O(n) per-render hash, also only feeding `.task(id:)`'s id). The actual expensive work (`ThemeExtractionService.termsBatch`, `BrainGraphBuilder.build`) is correctly gated inside `.task(id: taskKey(size))` and only re-fires when the id changes — not a bug, it's the fix pattern already in use elsewhere. `BrainViewModel.entries(for:in:)` does `.filter` + `.sorted` over the full-history array but is only invoked from the `.sheet(item: $selectedNode)` closure, i.e. once when a node detail sheet opens — same shape as the existing `EntryDetailView.onThisDayEntries` DISCARD (one-time cost per sheet-open, not per-frame). `BrainConstellationView.swift`/`BrainSceneView.swift` take a pre-built `BrainGraph` as input, no `@Query`, no full-history scan. `BrainGraph.swift`'s `.filter` lives inside `BrainGraphBuilder.build`, called from `Task.detached` in `BrainViewModel.rebuild`, not from any view `body`. Re-ran both scan methods over the rest of `Features/**/*.swift`: keyword grep and `@Query` inventory both returned the same files as Log [13]-[20] plus the now-triaged BrainView set. No new candidate found, no PR opened. — nothing found (2.0.4)

- [22] 2026-07-11 twelfth pass this run (new scheduled invocation, after BrainView triage in Log [21]): confirmed zero open PRs (`list_pull_requests` state=open → empty). `git log 40164d4..origin/2.0.4 --stat -- mirror/Features/` is empty — zero commits under `Features/` since the Log [21] checkpoint; only new commit since then is Log [21]'s own log entry (`40164d4`). Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the same file set as Log [21] (now also surfacing `BrainGraph.swift`/`BrainViewModel.swift` by keyword, but both were already read end-to-end and triaged non-candidate in Log [21] — `BrainGraph`'s `.filter` runs inside `Task.detached`, not a view body; `BrainViewModel.entries(for:in:)` only fires on sheet-open, one-time cost); `@Query` inventory (`grep -rln "@Query"`) returned the identical 9-file set as Log [13]-[21]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.4)

- [23] 2026-07-11 thirteenth pass this run (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.4` HEAD (`b46b98d`) matches `origin/2.0.4` exactly (`git log HEAD..origin/2.0.4`/`origin/2.0.4..HEAD` both empty) — no commits landed anywhere on the branch since Log [22]'s own log entry, let alone under `Features/`. Re-ran both scan methods once more as a sanity check: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the same 16-file set as Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[22]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.4)

- [24] 2026-07-11 fourteenth pass this run (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.4` HEAD (`32bf5dc`, Log [23]'s own commit) matches `origin/2.0.4` exactly — `git log 32bf5dc..origin/2.0.4` empty, no commits landed anywhere on the branch, let alone under `Features/`, since Log [23]. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]/[23]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[23]. No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.4)

- [25] 2026-07-12 fifteenth pass this run (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.4` HEAD (`848869f`, Log [24]'s own commit) matches `origin/2.0.4` exactly — `git log 848869f..origin/2.0.4` empty, no commits landed anywhere on the branch, let alone under `Features/`, since Log [24]. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[24]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[24]. No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.4)

- [26] 2026-07-12 first pass actually running against `2.0.5` (prior runs this same day, including Log [25], were still labeled/targeting `2.0.4` because the scheduled trigger's `job_config` hardcoded the branch — this session confirmed the trigger prompt now says `2.0.5` in all three places, and checked out `2.0.5` correctly per its instructions). Confirmed zero open PRs (`list_pull_requests` state=open → empty). `2.0.5` was cut fresh from `main` on 2026-07-10 (post PR #22) and carries every fix already merged through Log [21] (`entryCacheKey`-based caches in `MoodTimelineView.swift`/`InsightView.swift`/`AskView.swift`, confirmed present via `grep` — e.g. `cachedCurrentStreak`/`cachedConsecutiveNegativeCount`/`entryCacheKey` at `MoodTimelineView.swift:25-28`) plus the BrainView feature (`59e6998`) already triaged non-candidate in Log [21]. `git log --oneline -- mirror/Features/` on `2.0.5` shows zero commits under `Features/` since `59e6998` (BrainView) — nothing has landed there since the branch cut. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[25]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[25]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [27] 2026-07-12 second pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`cdaf43b`, Log [26]'s own commit) matches `origin/2.0.5` exactly (`git log HEAD..origin/2.0.5`/`origin/2.0.5..HEAD` both empty); `git log --oneline -- mirror/Features/` still shows zero commits since `59e6998` (BrainView) — nothing has landed under `Features/` since the branch cut. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[26]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[26]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [28] 2026-07-13 third pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`eeead50`, Log [27]'s own commit) matches `origin/2.0.5` exactly; `git log --oneline -- mirror/Features/` still shows zero commits since `59e6998` (BrainView) — nothing has landed under `Features/` since the branch cut. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[27]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[27]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [29] 2026-07-13 fourth pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`a2a34c2`, Log [28]'s own commit) matches `origin/2.0.5` exactly; `git log --oneline -- mirror/Features/` still shows zero commits since `59e6998` (BrainView) — nothing has landed under `Features/` since the branch cut. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[28]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[28]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [30] 2026-07-13 fifth pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`3118942`, Log [29]'s own commit) matches `origin/2.0.5` exactly (`git log HEAD..origin/2.0.5`/`origin/2.0.5..HEAD` both empty); `git log --oneline -- mirror/Features/` still shows zero commits since `59e6998` (BrainView) — nothing has landed under `Features/` since the branch cut. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[29]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[29]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [31] 2026-07-13 sixth pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`345e638`, Log [30]'s own commit) matches `origin/2.0.5` exactly (`git log HEAD..origin/2.0.5`/`origin/2.0.5..HEAD` both empty); `git log --oneline -- mirror/Features/` still shows zero commits since `59e6998` (BrainView) — nothing has landed under `Features/` since the branch cut. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[30]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[30]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [32] 2026-07-14 seventh pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`2891287`, Log [31]'s own commit) matches `origin/2.0.5` exactly (`git log HEAD..origin/2.0.5`/`origin/2.0.5..HEAD` both empty); `git log 345e638..HEAD --oneline -- mirror/Features/` still shows zero commits since `59e6998` (BrainView) — nothing has landed under `Features/` since the branch cut. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[31]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[31]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [33] 2026-07-14 eighth pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`a154587`, Log [32]'s own commit) matches `origin/2.0.5` exactly; `git log 59e6998..HEAD --oneline -- mirror/Features/` still shows zero commits since BrainView — nothing has landed under `Features/` since the branch cut. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[32]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[32]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [34] 2026-07-14 ninth pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`25741e2`, Log [33]'s own commit) matches `origin/2.0.5` exactly (`git log HEAD..origin/2.0.5`/`origin/2.0.5..HEAD` both empty); `git log a154587..HEAD --oneline -- mirror/Features/` still shows zero commits since the branch cut — nothing new has landed under `Features/`. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[33]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[33]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [35] 2026-07-14 tenth pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`470725a`, Log [34]'s own commit) matches `origin/2.0.5` exactly (`git log HEAD..origin/2.0.5`/`origin/2.0.5..HEAD` both empty); `git log 25741e2..HEAD --oneline -- mirror/Features/` still shows zero commits since the branch cut — nothing new has landed under `Features/`. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[34]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[34]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [36] 2026-07-15 eleventh pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`9ce9e0f`, Log [35]'s own commit) matches `origin/2.0.5` exactly (`git log HEAD..origin/2.0.5`/`origin/2.0.5..HEAD` both empty); `git log 470725a..HEAD --oneline -- mirror/Features/` still shows zero commits since the branch cut (`59e6998`, BrainView) — nothing new has landed under `Features/`. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[35]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[35]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [37] 2026-07-15 twelfth pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`272b816`, Log [36]'s own commit) matches `origin/2.0.5` exactly (`git log HEAD..origin/2.0.5`/`origin/2.0.5..HEAD` both empty); `git log 9ce9e0f..HEAD --oneline -- mirror/Features/` still shows zero commits since the branch cut (`59e6998`, BrainView) — nothing new has landed under `Features/`. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[36]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[36]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [38] 2026-07-15 thirteenth pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`fb1b9b9`, Log [37]'s own commit) matches `origin/2.0.5` exactly (`git log HEAD..origin/2.0.5`/`origin/2.0.5..HEAD` both empty); `git log 59e6998..HEAD --oneline -- mirror/Features/` still shows zero commits since the branch cut (BrainView) — nothing new has landed under `Features/`. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[37]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[37]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [39] 2026-07-16 fifteenth pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`cc9d4e4`, Log [38]'s own commit) matches `origin/2.0.5` exactly (`git log HEAD..origin/2.0.5`/`origin/2.0.5..HEAD` both empty); `git log 59e6998..HEAD --oneline -- mirror/Features/` still shows zero commits since the branch cut (BrainView) — nothing new has landed under `Features/`. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[38]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[38]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [40] 2026-07-16 sixteenth pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`1e630a8`, Log [39]'s own commit) matches `origin/2.0.5` exactly (`git log HEAD..origin/2.0.5`/`origin/2.0.5..HEAD` both empty); `git log 59e6998..HEAD --oneline -- mirror/Features/` still shows zero commits since the branch cut (BrainView) — nothing new has landed under `Features/`. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[39]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[39]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [41] 2026-07-16 seventeenth pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`671629b`, Log [40]'s own commit) matches `origin/2.0.5` exactly (`git log HEAD..origin/2.0.5`/`origin/2.0.5..HEAD` both empty); `git log 59e6998..HEAD --oneline -- mirror/Features/` still shows zero commits since the branch cut (BrainView) — nothing new has landed under `Features/`. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[40]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[40]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [42] 2026-07-16 eighteenth pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`e8dc521`, Log [41]'s own commit) matches `origin/2.0.5` exactly (`git log HEAD..origin/2.0.5`/`origin/2.0.5..HEAD` both empty); `git log 59e6998..HEAD --oneline -- mirror/Features/` still shows zero commits since the branch cut (BrainView) — nothing new has landed under `Features/`. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[41]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[41]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [43] 2026-07-17 nineteenth pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`203d003`, Log [42]'s own commit) matches `origin/2.0.5` exactly (`git log HEAD..origin/2.0.5`/`origin/2.0.5..HEAD` both empty); `git log 59e6998..HEAD --oneline -- mirror/Features/` still shows zero commits since the branch cut (BrainView) — nothing new has landed under `Features/`. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[42]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[42]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [44] 2026-07-17 twentieth pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`69c2147`, Log [43]'s own commit) matches `origin/2.0.5` exactly (`git log HEAD..origin/2.0.5`/`origin/2.0.5..HEAD` both empty); `git log 59e6998..HEAD --oneline -- mirror/Features/` still shows zero commits since the branch cut (BrainView) — nothing new has landed under `Features/`. Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[43]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[43]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [45] 2026-07-17 twenty-first pass on `2.0.5` (new scheduled invocation): confirmed zero open PRs (`list_pull_requests` state=open → empty). Local `2.0.5` HEAD (`935dc12`, Log [44]'s own commit) matches `origin/2.0.5` exactly (`git log 59e6998..HEAD --oneline -- mirror/Features/` empty, 41 total commits since branch cut are all loop-state log entries, no `Features/` changes). Re-ran both scan methods once more: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as Log [22]-[44]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as Log [13]-[44]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.5)

- [46] 2026-07-18 first pass on `2.0.6` (new scheduled invocation, branch cut from `2.0.5` per the trigger update noted 2026-07-18 above): confirmed via `list_pull_requests` there is no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged). Local `2.0.6` HEAD (`023fb33`) matches `origin/2.0.6` exactly. `git log origin/2.0.5..origin/2.0.6 -- mirror/Features/` is empty — `2.0.6` carries zero `Features/` changes beyond what `2.0.5` already had (fastlane/metadata commits only); `git log 59e6998..HEAD -- mirror/Features/` (BrainView, the last real `Features/` change) is also empty. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [47] 2026-07-19 second pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged, unchanged since Log [46]). Local `2.0.6` HEAD (`836b3f2`, Log [46]'s own commit) matches `origin/2.0.6` exactly (`git log HEAD..origin/2.0.6`/`origin/2.0.6..HEAD` both empty). `git log 023fb33..HEAD -- mirror/Features/` (Log [46]'s checkpoint) is empty — zero new commits under `Features/` since then. Noted for the record: `git log 59e6998..HEAD -- mirror/Features/` (BrainView, the last real pre-existing `Features/` change) is *not* actually empty as Log [46] stated — it contains two commits (`56d09d5` BrainViewModel state-machine fix, `60e9ac2` SettingsView+Sections badge fix), both already ancestors of Log [46]'s own checkpoint and both already fully described in this file's "Bug reports" section (2026-07-18 FIXED) and PR #23's description; Log [46]'s claim of "empty" was inaccurate but harmless since both commits were already triaged elsewhere. Neither is a full-history-scan-in-body candidate: `BrainViewModel.swift`'s change only touches the `.ready`-vs-any-terminal-state early-return condition (control flow, not a new scan), and `SettingsView+Sections.swift`'s change only swaps a badge icon/label based on `allFeaturesFree` (no `@Query`, no collection scan). Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No new candidate found, no PR opened. — nothing found (2.0.6)

- [48] 2026-07-19 third pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged, unchanged since Log [47]). Local `2.0.6` HEAD (`8ca5d89`, Log [47]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical) — zero new commits anywhere on the branch, let alone under `Features/`, since Log [47]. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No new candidate found, no PR opened. — nothing found (2.0.6)

- [49] 2026-07-19 fourth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged, unchanged since Log [47]/[48]). Local `2.0.6` HEAD (`96c5dc5`, Log [48]'s own commit) matches `origin/2.0.6` exactly; `git log 8ca5d89..HEAD -- mirror/Features/` (Log [48]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [50] 2026-07-19 fifth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged, unchanged since Log [47]-[49]). Local `2.0.6` HEAD (`62d5bf5`, Log [49]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 96c5dc5..HEAD -- mirror/Features/` (Log [49]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [51] 2026-07-20 sixth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged, unchanged since Log [47]-[50]). Local `2.0.6` HEAD (`7b626b1`, Log [50]'s own commit) matches `origin/2.0.6` exactly; `git log 62d5bf5..HEAD -- mirror/Features/` (Log [50]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [52] 2026-07-20 seventh pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged, unchanged since Log [47]-[51]). Local `2.0.6` HEAD (`1e58dab`, Log [51]'s own commit) matches `origin/2.0.6` exactly; `git log 7b626b1..HEAD -- mirror/Features/` (Log [50]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [53] 2026-07-21 eighth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged, unchanged since Log [47]-[52]). Local `2.0.6` HEAD (`b762a77`, Log [52]'s own commit) matches `origin/2.0.6` exactly; `git log 1e58dab..HEAD -- mirror/Features/` (Log [52]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [54] 2026-07-21 ninth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged, unchanged since Log [47]-[53]). Local `2.0.6` HEAD (`9500627`, Log [53]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log b762a77..HEAD -- mirror/Features/` (Log [53]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [55] 2026-07-22 tenth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged, unchanged since Log [47]-[54]). Local `2.0.6` HEAD (`707c4f7`, Log [54]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 9500627..HEAD -- mirror/Features/` (Log [54]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [56] 2026-07-22 eleventh pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged, unchanged since Log [47]-[55]). Local `2.0.6` HEAD (`6e47a55`, Log [55]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 707c4f7..HEAD -- mirror/Features/` (Log [55]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [57] 2026-07-22 twelfth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged, unchanged since Log [47]-[56]). Local `2.0.6` HEAD (`e760320`, Log [56]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 6e47a55..HEAD -- mirror/Features/` (Log [56]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [58] 2026-07-22 thirteenth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged, unchanged since Log [47]-[57]). Local `2.0.6` HEAD (`3dcffeb`, Log [57]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log e760320..HEAD -- mirror/Features/` (Log [57]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [59] 2026-07-23 fourteenth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged, unchanged since Log [47]-[58]). Local `2.0.6` HEAD (`312cc95`, Log [58]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 3dcffeb..HEAD -- mirror/Features/` (Log [58]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [60] 2026-07-23 fifteenth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged, unchanged since Log [47]-[59]). Local `2.0.6` HEAD (`804fd3a`, Log [59]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 312cc95..HEAD -- mirror/Features/` (Log [59]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [61] 2026-07-24 sixteenth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged, unchanged since Log [47]-[60]). Local `2.0.6` HEAD (`fb2ac4a`, Log [60]'s own commit) matches `origin/2.0.6` exactly; `git log 804fd3a..HEAD -- mirror/Features/` (Log [60]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [62] 2026-07-24 seventeenth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged, unchanged since Log [47]-[61]). Local `2.0.6` HEAD (`3c6cd50`, Log [61]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log fb2ac4a..HEAD -- mirror/Features/` (Log [61]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [63] 2026-07-24 eighteenth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged, unchanged since Log [47]-[62]). Local `2.0.6` HEAD (`ece7c6f`, Log [62]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 3c6cd50..HEAD -- mirror/Features/` (Log [62]'s checkpoint) is empty — zero new commits under `Features/` since then, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [64] 2026-07-25 nineteenth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, unchanged since Log [47]-[63]). Local `2.0.6` HEAD (`5720c3e`, Log [63]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log ece7c6f..HEAD -- mirror/Features/` (Log [63]'s checkpoint) is empty — zero new commits under `Features/` since then, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [65] 2026-07-25 twentieth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, unchanged since Log [47]-[64]). Local `2.0.6` HEAD (`d6ec008`, Log [64]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log ece7c6f..HEAD -- mirror/Features/` (Log [63]'s checkpoint) is empty — zero new commits under `Features/` since then, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter { `/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

## Blocked

- InsightView.swift dominantMood(ThisWeek)'s own `Dictionary(grouping:)` step, MonthlyReportView.swift topMood, EntryDetailView.swift onThisDayEntries — not a hot path by the seed heuristic's own definition (per-frame recompute on unrelated state). Outer-loop adjustment: the `Dictionary(grouping:)`/`.filter` grep is too broad a seed signal on its own — future scans must also check (a) dataset scope (week/month vs full history) and (b) whether the containing view has frequently-churning unrelated `@State` that retriggers body eval, AND (c, added 2026-07-07) trace one level *into* the inputs of a cheap-looking computed property — a small-input step can still sit downstream of an uncached full-history scan (see Log [6]). Don't re-add without new evidence.
- MonthlyReportView.swift `selectedMonthEntries` (full-history `entries.filter`) and `cachedReportForSelectedMonth` (full-history `insights.first` scan) — DISCARD (2026-07-08, Log [12], no code touched) — both full-history scans, but the view's only unrelated `@State` (`showPaywall`) never re-triggers a branch that reads them; `selectedMonth` changes are the legitimate, expected trigger for recomputing a month-scoped view, not unrelated churn. Don't re-add without evidence of a churning `@State` that retriggers these reads.

## Status

All loop PRs closed out: PR #18 KEEP-then-superseded (its cache was removed with the per-entry bar product decision below), PR #19 merged (`6b7fa39`, Log [7]), PR #20 merged with a cache-key fix (`0302b61`, Log [8]), PR #21 merged (Log [20], after nine automated re-checks in Log [11]-[19] found it sitting unreviewed for 48h+). No open loop branches. Backlog's remaining 2026-07-06 candidates are all logged DISCARD/Blocked, and `MonthlyReportView.swift`'s full-history reads are also DISCARD (Log [12]). Every `@Query`-touching file in `Features/**/*.swift` has been read end-to-end at least once (see Log [12]) — future runs should still re-scan for new/changed files, but no longer need to re-derive the `@Query` inventory from scratch. Re-scan `Features/**/*.swift` fresh next run rather than trusting this file's stale candidate list, and apply the Log [8] cache-key lesson to any new `.task(id:)` proposals.

Separately (not a loop item): BrainView feature (new Insights visualization, `BrainView`/`BrainConstellationView`/`BrainSceneView`/`BrainGraph`/`BrainViewModel` + `ThemeExtractionService`) was committed into 2.0.4 in the same session as the PR #21 merge (Log [20]), build+test verified. Also note: `SubscriptionService.allFeaturesFree = true` — app currently ships in "early access" mode with all tiers unlocked and no real IAP; `b17fe0d`/`b843321` (2026-07-08) removed dangling paid-tier UI (Manage Subscription button, fake Deep checkmark) that this flag was leaving behind.

2026-07-10: `2.0.4` was PR'd into `main` (#22) and merged — `main` is now the up-to-date superset of all real work (every other branch checked; only `1.0.2` had unique commits, confirmed stale duplicates already reflected in `main` via rebased `1.0.3` commits). **Active development branch is now `2.0.5`**, cut fresh from `main`. `MARKETING_VERSION` bumped 2.0.4 → 2.0.5 and `CURRENT_PROJECT_VERSION` reset to 1 (app + widget extension, kept matching) on both app and widget targets. Future loop runs should target `2.0.5`, not `2.0.4`.

2026-07-12: found the actual reason loop runs kept targeting `2.0.4` after the above note — this file's note was never the source of truth for the cloud routine. The scheduled trigger (`trig_011YwbGGDjd2PYVWP73maBEP`, runs every 6h) has the branch **hardcoded three times directly in its job_config prompt content** (`git checkout 2.0.4`, `open a PR into 2.0.4`, `commit directly to 2.0.4`) — the routine never reads this state file's prose notes for its target branch, only the branch name baked into its own instructions. Updated the trigger via `RemoteTrigger action=update` (full job_config resend, per the existing "ccr replaces wholesale" lesson) to say `2.0.5` in all three places. **Lesson: a note in loop-state.md does NOT change cron/trigger behavior — if the target branch changes, the RemoteTrigger job_config itself must be updated, not just this file.**

2026-07-18: `2.0.5` was PR'd into `main` (#23, not yet merged — awaiting review, so `main` does not yet contain 2.0.5's work). **Active development branch is now `2.0.6`**, cut directly from `2.0.5` (not from `main`, since the PR hasn't merged — branching from `2.0.5` carries the same content main will have post-merge without waiting). `MARKETING_VERSION` bumped 2.0.5 → 2.0.6 on app + widget targets; `CURRENT_PROJECT_VERSION` left at 1 (no prior build uploaded at 2.0.6 yet). Applied the Log [above] lesson immediately this time: updated `trig_011YwbGGDjd2PYVWP73maBEP`'s job_config (all three hardcoded branch refs) to `2.0.6` via full `RemoteTrigger action=update` resend, in the same session as the branch cut — future loop runs should target `2.0.6`. Also added `fastlane/Fastfile` with a `release` lane (build_app + upload_to_app_store with submit_for_review) and ran it for 2.0.5 in this session — check PR #23 / App Store Connect for outcome, not this file (release status isn't tracked here).

## Bug reports (user-observed)

- 2026-07-12: Brain View (Insights tab) hangs indefinitely on "Mapping your mind…" spinner — user screenshot, real device. Never resolves to graph/empty/notEnoughEntries state; no crash. Original leading theory (NLTagger `enumerateTags` hang on bad input) was never repro'd.
  **FIXED 2026-07-18**: actual root cause was a state-machine bug, not NLTagger. Every terminal branch in `BrainView.stateContent` (`.notEnoughEntries`, `.empty`, `.ready`) carries its own `.task(id: taskKey(size))`, which fires on mount. `BrainViewModel.rebuild()` (`BrainViewModel.swift:22`) only short-circuited on a fingerprint match when `state` was already `.ready` — so landing on `.empty` (no recurring terms) re-triggered `rebuild()`, which unconditionally reset `state = .loading` before recomputing, remounting the loading branch's task, which called `rebuild()` again — an infinite `.loading ↔ .empty` loop that renders as a permanently stuck spinner. Fix: the fingerprint-match early-return no longer requires `.ready`, it returns for any terminal state (`BrainViewModel.swift:22-31`). Verified: full build + `mirrorTests` (incl. `BrainViewTests`) green.

## Product decision (2026-07-07)

- WriteView word-goal bar is now **per-entry** (`viewModel.wordCount / dailyWordGoal`), by user decision — the daily cumulative `dailyWordCount` + `cachedSavedWordCountToday` cache (PR #18) was removed. Do NOT re-propose caching a daily word count in WriteView; the full-history scan no longer exists. `test_dailyWordCount_perKeystrokeVsCached` kept as a standalone benchmark.

- [66] 2026-07-25 twenty-first pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, unchanged since Log [47]-[65]). Local `2.0.6` HEAD (`76997303f0ddd64692653e44865906b303dd20a7`, Log [65]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log ece7c6f..HEAD -- mirror/Features/` (Log [63]'s checkpoint) is empty — zero new commits under `Features/` since then, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [67] 2026-07-26 twenty-second pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` unchanged since Log [47]). Local `2.0.6` HEAD (`0ad5257`, Log [66]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 76997303..HEAD -- mirror/Features/` (Log [66]'s checkpoint) is empty — zero new commits under `Features/` since then, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [68] 2026-07-26 twenty-third pass on `2.0.6` (new scheduled invocation, ~6h after Log [67]): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` unchanged since Log [47]). Local `2.0.6` HEAD (`5e3ad44`, Log [67]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 76997303..HEAD -- mirror/Features/` is empty — zero new commits under `Features/` since Log [66]'s checkpoint, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [69] 2026-07-26 twenty-fourth pass on `2.0.6` (new scheduled invocation, ~6h after Log [68]): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` unchanged since Log [47]). Local `2.0.6` HEAD (`0a60347`, Log [68]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 76997303..HEAD -- mirror/Features/` is empty — zero new commits under `Features/` since Log [66]'s checkpoint, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [70] 2026-07-26 twenty-fifth pass on `2.0.6` (new scheduled invocation, ~6h after Log [69]): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` unchanged since Log [47] — now over a week with zero review activity). Local `2.0.6` HEAD (`12e9b89`, Log [69]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 0a60347..HEAD -- mirror/Features/` is empty — zero new commits under `Features/` since Log [69]'s checkpoint, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [71] 2026-07-27 twenty-sixth pass on `2.0.6` (new scheduled invocation, ~24h after Log [70]): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` unchanged since Log [47] — now over 9 days with zero review activity). Local `2.0.6` HEAD (`c2789c3`, Log [70]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 12e9b89..HEAD -- mirror/Features/` is empty — zero new commits under `Features/` since Log [70]'s checkpoint, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [72] 2026-07-27 twenty-seventh pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` unchanged since Log [47] — now over 9 days with zero review activity). Local `2.0.6` HEAD (`ba9ce3a`, Log [71]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 12e9b89..HEAD -- mirror/Features/` is empty — zero new commits under `Features/` since Log [70]'s checkpoint, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [73] 2026-07-27 twenty-eighth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` still `2026-07-18T10:38:54Z` — unchanged since Log [47], now over 9 days with zero review activity). Local `2.0.6` HEAD (`b5e0458`, Log [72]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log ba9ce3a..HEAD -- mirror/Features/` is empty — zero new commits under `Features/` since Log [71]'s checkpoint, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [74] 2026-07-27 twenty-ninth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` still `2026-07-18T10:38:54Z` — unchanged since Log [47], now over 9 days with zero review activity). Local `2.0.6` HEAD (`5f89d3a`, Log [73]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log b5e0458..HEAD -- mirror/Features/` is empty — zero new commits under `Features/` since Log [72]'s checkpoint, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [75] 2026-07-28 thirtieth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` still `2026-07-18T10:38:54Z` — unchanged since Log [47], now 10 days with zero review activity). Local `2.0.6` HEAD (`fddfc7d`, Log [74]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 5f89d3a..HEAD -- mirror/Features/` is empty — zero new commits under `Features/` since Log [73]'s checkpoint, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [76] 2026-07-28 thirty-first pass on `2.0.6` (new scheduled invocation, ~6h after Log [75]): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` still `2026-07-18T10:38:54Z` — unchanged since Log [47], now 10 days with zero review activity). Local `2.0.6` HEAD (`03af464`, Log [75]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 03af464..HEAD -- mirror/Features/` is empty — zero new commits under `Features/` since Log [75]'s checkpoint, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [77] 2026-07-28 thirty-second pass on `2.0.6` (new scheduled invocation, ~6h after Log [76]): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` still `2026-07-18T10:38:54Z` — unchanged since Log [47], now 10 days with zero review activity). Local `2.0.6` HEAD (`e9a6c99`, Log [76]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files, and `git log 03af464..HEAD -- mirror/Features/` (Log [76]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [78] 2026-07-28 thirty-third pass on `2.0.6` (new scheduled invocation, ~6h after Log [77]): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` still `2026-07-18T10:38:54Z` — unchanged since Log [47], now 10 days with zero review activity). Local `2.0.6` HEAD (`f4caba7`, Log [77]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files, and `git log e9a6c99..HEAD -- mirror/Features/` (Log [77]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [79] 2026-07-29 thirty-fourth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` still `2026-07-18T10:38:54Z` — unchanged since Log [47], now 11 days with zero review activity). Local `2.0.6` HEAD (`f8a53ad`, Log [78]'s own commit) matches `origin/2.0.6` exactly; `git log f4caba7..HEAD -- mirror/Features/` (Log [78]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [80] 2026-07-29 thirty-fifth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` still `2026-07-18T10:38:54Z` — unchanged since Log [47], now 11 days with zero review activity). Local `2.0.6` HEAD (`c3528bb`, Log [79]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files, and `git log f4caba7..HEAD -- mirror/Features/` (Log [78]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [81] 2026-07-29 thirty-sixth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` still `2026-07-18T10:38:54Z` — unchanged since Log [47], now 11 days with zero review activity). Local `2.0.6` HEAD (`7ee6c3c`, Log [80]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files, and `git log c3528bb..HEAD -- mirror/Features/` (Log [79]'s checkpoint) is empty — zero new commits under `Features/` since then. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [82] 2026-07-29 thirty-seventh pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` still `2026-07-18T10:38:54Z` — unchanged since Log [47], now 11 days with zero review activity). Local `2.0.6` HEAD (`b2e285c`, Log [81]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 7ee6c3c..HEAD -- mirror/Features/` (Log [81]'s checkpoint) is empty — zero new commits under `Features/` since then, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [83] 2026-07-30 thirty-eighth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` still `2026-07-18T10:38:54Z` — unchanged since Log [47], now 12 days with zero review activity). Local `2.0.6` HEAD (`70e2ae7`, Log [82]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log b2e285c..HEAD -- mirror/Features/` (Log [82]'s checkpoint) is empty — zero new commits under `Features/` since then, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [84] 2026-07-30 thirty-ninth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` still `2026-07-18T10:38:54Z` — unchanged since Log [47], now 12 days with zero review activity). Local `2.0.6` HEAD (`7df7689`, Log [83]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 70e2ae7..HEAD -- mirror/Features/` (Log [83]'s checkpoint) is empty — zero new commits under `Features/` since then, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]; total `.swift` file count under `Features/` (36) also unchanged. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [85] 2026-07-30 fortieth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` still `2026-07-18T10:38:54Z` — unchanged since Log [47], now 12 days with zero review activity). Local `2.0.6` HEAD (`1ef8b70`, Log [84]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 70e2ae7..HEAD -- mirror/Features/` (Log [83]'s checkpoint) is empty — zero new commits under `Features/` since then, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]; total `.swift` file count under `Features/` (36) also unchanged. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [86] 2026-07-30 forty-first pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` still `2026-07-18T10:38:54Z` — unchanged since Log [47], now 12 days with zero review activity). Local `2.0.6` HEAD (`9ecda64`, Log [85]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 1ef8b70..HEAD --oneline` shows only Log [85]'s own log commit — zero new commits under `Features/` since then, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]; total `.swift` file count under `Features/` (36) also unchanged. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [87] 2026-07-31 forty-second pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` still `2026-07-18T10:38:54Z` — unchanged since Log [47], now 13 days with zero review activity). Local `2.0.6` HEAD (`3bf968b`, Log [86]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 9ecda64..HEAD --oneline -- mirror/Features/` is empty — zero new commits under `Features/` since Log [86]'s checkpoint, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [88] 2026-07-31 forty-third pass on `2.0.6` (new scheduled invocation, ~6h after Log [87]): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` still `2026-07-18T10:38:54Z` — unchanged since Log [47], now 13 days with zero review activity). Local `2.0.6` HEAD (`23a92dd`, Log [87]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 9ecda64..HEAD --oneline -- mirror/Features/` is empty — zero new commits under `Features/` since Log [86]'s checkpoint, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [89] 2026-08-01 forty-fourth pass on `2.0.6` (new scheduled invocation): confirmed via `list_pull_requests` there is still no open PR targeting `2.0.6` (only PR #23, `2.0.5`→`main`, still open/unmerged since 2026-07-18, `updated_at` still `2026-07-18T10:38:54Z` — unchanged since Log [47], now 14 days with zero review activity). Local `2.0.6` HEAD (`b7103b6`, Log [88]'s own commit) matches `origin/2.0.6` exactly (`git rev-parse HEAD origin/2.0.6` identical); `git log 23a92dd..HEAD --oneline -- mirror/Features/` is empty — zero new commits under `Features/` since Log [87]'s checkpoint, and `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files either; `.swift` file count under `Features/` (36) unchanged. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) returned the identical 16-file set as every pass since Log [22]; `@Query` inventory (`grep -rln "@Query"`) returned the identical 10-file set as every pass since Log [13]. No source file has changed since the last full read-through, so no new candidate is possible without new evidence. Found nothing, no new PR opened. — nothing found (2.0.6)

- [90] 2026-09-05 first pass on `2.1.0` (new scheduled invocation; job now targets `2.1.0`, cut some time after `2.0.6` and carrying substantial new `Features/` work — Sentinel mode, daily mood check-ins via `MoodLog`/`MoodCheckIn`, rate-us gate, and a Settings split into `ArchiveSettingsView.swift`/`DiagnosticsSettingsView.swift`/`ManualSettingsView.swift`/`ProtocolSettingsView.swift`/`SettingsComponents.swift`). Confirmed zero open PRs before starting (`list_pull_requests` state=open → empty). Re-verified the previously-fixed files (`MoodTimelineView.swift`, `InsightView.swift`, `AskView.swift`, `EntryListView.swift`, `CalendarHeatmap.swift`) survived the intervening changes with their `.task(id:)` caches intact (e.g. `MoodTimelineView.swift` now also merges `MoodCheckIn` data into the same `moodDataCacheKey`/`cachedCurrentStreak` caches — correctly extended, not regressed). Ran both scan methods fresh on the new file set: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) surfaced one new hit not previously triaged, `Settings/ArchiveSettingsView.swift`; `@Query` inventory (`grep -rln "@Query"`) surfaced two new files, `ArchiveSettingsView.swift` and `DiagnosticsSettingsView.swift`. `DiagnosticsSettingsView.swift`'s `entries.filter(\.textDecryptionFailed).count` only runs inside a Button action closure (a diagnostics report, DEBUG-only), not from `body` — not a candidate. `ArchiveSettingsView.swift`'s `exportedText` is a genuine hit: it decrypts every entry's `text` (and formats a date) via `entries.map { ... entry.text ... }`, read directly in `body` via `ShareLink(item: exportedText, ...)`, in a view that holds several unrelated `@State` vars that churn independently (`showDeleteConfirmation`, `showImportPicker`, `importResultMessage`, `showImportResult`, `iCloudStatus` set once via `.task`) — same bug shape as the prior `AskView`/`InsightView` fixes, and arguably a cleaner match since it's literal per-entry decryption, not just `.filter`/`.sorted`. Cached into `@State cachedExportedText` via `.task(id: exportCacheKey)`, keying on the raw `encryptedText`/`encryptedMood` ciphertext fields (not the decrypted `text`/`mood`) so computing the key itself never decrypts — same lesson as the `entryCacheKey` precedent (Log [8]), extended to cover a full-text field rather than just mood+date. Added `test_archiveSettingsExportedText_perToggleVsCached` to `PerformanceXCTests.swift` (Test 10), modeled on Test 9. NOT build/test-verified (no Xcode/simulator in this sandbox) — awaiting local xcodebuild + mirrorTests run. — proposed (2.1.0, PR #28, branch `loop/archivesettings-exportedtext-cache`)

- [91] 2026-09-05 second pass on `2.1.0` (new scheduled invocation, shortly after Log [90]): confirmed via `list_pull_requests` that PR #28 (`loop/archivesettings-exportedtext-cache` → `2.1.0`) is still open, `updated_at` unchanged since creation (`2026-09-05T00:06:00Z`) — no review/comment/push activity yet. Local `2.1.0` HEAD (`4a10619`, Log [90]'s own commit) matches `origin/2.1.0` exactly; `git status --porcelain --untracked-files=all -- mirror/Features/` confirms no untracked files. Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) and `@Query` inventory (`grep -rln "@Query"`) both returned the identical file set Log [90] already triaged (incl. `ArchiveSettingsView.swift`/`DiagnosticsSettingsView.swift`, both already resolved — cached vs. DEBUG-only button action). No source file has changed since Log [90], so no new candidate is possible without new evidence. Found nothing, no new PR opened. PR #28 remains the sole pending item, awaiting human local xcodebuild/mirrorTests verification and merge. — nothing found (2.1.0)

- [92] 2026-09-05 third pass on `2.1.0` (new scheduled invocation): confirmed via `list_pull_requests` that PR #28 (`loop/archivesettings-exportedtext-cache` → `2.1.0`) is still the only open PR, `updated_at` unchanged since creation (`2026-09-05T00:06:00Z`) — no review/comment/push activity yet. Local `2.1.0` HEAD moved to `01aeccc` since Log [91]'s checkpoint (`4a10619`), but the three intervening commits (`770fadc` Log [91]'s own log entry, `3d5d56e` a launch-arg rename + plan-doc correction for an unrelated "Track B2" digest-UI effort, `eebf075` a one-line `.textCase(.uppercase)` fix to `WeeklyDigestView.swift`'s header label plus DEBUG-only `SampleData.swift` seeding, `01aeccc` a doc-only plan correction) touch no full-history `@Query` computed property — `git diff --stat 4a10619..HEAD -- mirror/Features/` shows only the single-line `WeeklyDigestView.swift` header-label change, which doesn't read `entries`/`insights` at all (it's a static string modifier on a section title). Re-ran both scan methods fresh: keyword grep (`.sorted(`/`.filter {`/`Dictionary(grouping:`/`decrypt`) and `@Query` inventory (`grep -rln "@Query"`) both returned the identical file set as Log [90]/[91] (43 `.swift` files under `Features/`, unchanged), all previously triaged. No source file relevant to this bug shape has changed since Log [90], so no new candidate is possible without new evidence. Found nothing, no new PR opened. PR #28 remains the sole pending item, still awaiting human local xcodebuild/mirrorTests verification and merge. — nothing found (2.1.0)
